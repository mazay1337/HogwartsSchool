package ru.hogwarts.school.service;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.junit.jupiter.MockitoExtension;

@ExtendWith(MockitoExtension.class)
public class StudentServiceTest {

    @Test
    void testCreate() {
/*        // Arrange
        Map<Long, Student> map = new HashMap<>();
        //StudentService studentService = new StudentService(map);
        //Student student = new Student("Alice", 20);

        // Act
        Student createdStudent = studentService.create(student);

        // Assert
        assertNotNull(createdStudent); // Check if the created student is not null
        assertEquals("Alice", createdStudent.getName()); // Check if the name is correct
        assertEquals(20, createdStudent.getAge()); // Check if the age is correct
        assertTrue(map.containsKey(createdStudent.getId())); // Check if the student is added to the map
        assertEquals(student, map.get(createdStudent.getId())); // Check if the student in the map is the same as the created student
        */
    }

}
